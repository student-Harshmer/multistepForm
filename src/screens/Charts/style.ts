import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingTop: 50,
    paddingLeft: 10,
  },
  centeredText: {
    textAlign: 'center',
    paddingVertical: 20,
  },
});