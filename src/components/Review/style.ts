import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    gap: 10,
  },
  headerText: {
    fontSize: 20,
    fontWeight: 600,
    textAlign: 'center',
    paddingBottom: 20,
  },
  detailsContainer: {
    flexDirection: 'row',
    gap: 5,
  },
  valueTextContainer: {
    paddingRight: 40,
  },
  linkText: {
    color: '#007AFF',
  },
  inputKey: {
    fontSize: 16,
    fontWeight: 500,
  }
});